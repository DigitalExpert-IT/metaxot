import { LayoutMain } from "components";
import { SectionHeader } from "components/pages";

function Home() {
  return (
    <LayoutMain>
      <SectionHeader />
    </LayoutMain>
  );
}

export default Home;
