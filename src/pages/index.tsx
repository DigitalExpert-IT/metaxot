import { LayoutMain } from "components";
import { SectionHeader, SectionMissionCard } from "components/pages";

function Home() {
  return (
    <LayoutMain>
      <SectionHeader />
      <SectionMissionCard />
    </LayoutMain>
  );
}

export default Home;
