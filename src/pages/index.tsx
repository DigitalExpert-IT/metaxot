import { LayoutMain } from "components";
import { SectionHeader, SectionAboutUs } from "components/pages";

function Home() {
  return (
    <LayoutMain>
      <SectionHeader />
      <SectionAboutUs />
    </LayoutMain>
  );
}

export default Home;
